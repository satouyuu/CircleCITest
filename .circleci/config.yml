# .circleci/config.yml
version: 2
jobs:
  hello-world:
    macos:
      xcode: "10.0.0"

    steps:
      - checkout
      - run:
          command: echo 'Hello World !!'

workflows:
  version: 2
  hello-world:
    jobs:
      - hello-world
